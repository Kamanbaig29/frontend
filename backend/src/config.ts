// import dotenv from "dotenv";
// import { PublicKey } from "@solana/web3.js";

// dotenv.config();

// export const MEMEHOME_PROGRAM_ID = new PublicKey(
//   process.env.MEMEHOME_PROGRAM_ID || ""
// );

// export const RPC_ENDPOINT = process.env.RPC_ENDPOINT || "";
// export const RPC_WEBSOCKET_ENDPOINT = process.env.RPC_WEBSOCKET_ENDPOINT || "";

// if (!MEMEHOME_PROGRAM_ID || !RPC_ENDPOINT || !RPC_WEBSOCKET_ENDPOINT) {
//   throw new Error("❌ .env file is missing one or more required values.");
// } else {
//     console.log("✅ .env file loaded successfully.");
//     console.log(`MEMEHOME_PROGRAM_ID: ${MEMEHOME_PROGRAM_ID}`);
//     console.log(`RPC_ENDPOINT: ${RPC_ENDPOINT}`);
//     console.log(`RPC_WEBSOCKET_ENDPOINT: ${RPC_WEBSOCKET_ENDPOINT}`);
// }
